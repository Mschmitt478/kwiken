diff --git a/chrome/app/generated_resources.grd b/chrome/app/generated_resources.grd
index 8e771c64683fc0cfd0861f603efca1385c8ed40b..6881984f3bbb0f7561835ed3fe63bbf68758ee25 100644
--- a/chrome/app/generated_resources.grd
+++ b/chrome/app/generated_resources.grd
@@ -12071,6 +12071,12 @@ Check your passwords anytime in <ph name="GOOGLE_PASSWORD_MANAGER">$1<ex>Google
         <message name="IDS_TAB_CXMENU_UNPIN_TAB" desc="The label of the tab context menu item for unpinning one or more tabs.">
           Unpin
         </message>
+        <message name="IDS_TAB_CXMENU_ADD_TO_ESSENTIALS" desc="The label of the Kwiken tab context menu item that adds one or more tabs to the persistent Essentials grid.">
+          Add to Essentials
+        </message>
+        <message name="IDS_TAB_CXMENU_REMOVE_FROM_ESSENTIALS" desc="The label of the Kwiken tab context menu item that removes one or more tabs from the persistent Essentials grid.">
+          Remove from Essentials
+        </message>
         <message name="IDS_TAB_CXMENU_SOUND_MUTE_SITE" desc="The label of the tab context menu item for muting one or more sites. NOTE: If having no explicit # makes the phrasing awkward, feel free to add a # as necessary. [ICU Syntax]">
           {NUM_TABS, plural, =1 {Mute site} other {Mute sites}}
         </message>
@@ -12193,6 +12199,12 @@ Check your passwords anytime in <ph name="GOOGLE_PASSWORD_MANAGER">$1<ex>Google
         <message name="IDS_TAB_CXMENU_UNPIN_TAB" desc="In Title Case: The label of the tab context menu item for unpinning one or more tabs.">
           Unpin
         </message>
+        <message name="IDS_TAB_CXMENU_ADD_TO_ESSENTIALS" desc="In Title Case: The label of the Kwiken tab context menu item that adds one or more tabs to the persistent Essentials grid.">
+          Add to Essentials
+        </message>
+        <message name="IDS_TAB_CXMENU_REMOVE_FROM_ESSENTIALS" desc="In Title Case: The label of the Kwiken tab context menu item that removes one or more tabs from the persistent Essentials grid.">
+          Remove from Essentials
+        </message>
         <message name="IDS_TAB_CXMENU_SOUND_MUTE_SITE" desc="In Title Case: The label of the tab context menu item for muting one or more sites. NOTE: If having no explicit # makes the phrasing awkward, feel free to add a # as necessary. [ICU Syntax]">
           {NUM_TABS, plural, =1 {Mute Site} other {Mute Sites}}
         </message>
diff --git a/chrome/browser/ui/tabs/tab_menu_model.cc b/chrome/browser/ui/tabs/tab_menu_model.cc
index 2c39030db7a43c0d4ef0f985fe5b1bdca7c66cd0..9fb56f16a7d3784e38ab8ae923fea9fbfb42d121 100644
--- a/chrome/browser/ui/tabs/tab_menu_model.cc
+++ b/chrome/browser/ui/tabs/tab_menu_model.cc
@@ -33,6 +33,7 @@
 #include "chrome/browser/ui/tabs/existing_window_sub_menu_model.h"
 #include "chrome/browser/ui/tabs/features.h"
 #include "chrome/browser/ui/tabs/glic_tab_sub_menu_model.h"
+#include "chrome/browser/ui/tabs/pinned_tab_service_factory.h"
 #include "chrome/browser/ui/tabs/split_tab_menu_model.h"
 #include "chrome/browser/ui/tabs/split_tab_swap_menu_model.h"
 #include "chrome/browser/ui/tabs/split_view_layout_menu_model.h"
@@ -461,10 +462,19 @@ void TabMenuModel::Build(int index) {
                       IDS_TAB_CXMENU_DUPLICATE);
   SetElementIdentifierAt(GetItemCount() - 1, kDuplicateMenuItem);
 
-  bool will_pin = tab_strip_->WillContextMenuPin(index);
-  AddItemWithStringId(
-      TabStripModel::CommandTogglePinned,
-      will_pin ? IDS_TAB_CXMENU_PIN_TAB : IDS_TAB_CXMENU_UNPIN_TAB);
+  const bool will_pin = tab_strip_->WillContextMenuPin(index);
+  // Kwiken's durable pinned grid is its Essentials tier. Keep Chromium's
+  // terminology in windows and profiles that the persistence service does not
+  // own so the menu never promises durability that is unavailable there.
+  const bool uses_essentials =
+      tab_strip_->delegate()->IsNormalWindow() &&
+      PinnedTabServiceFactory::GetForProfile(tab_strip_->profile()) != nullptr;
+  const int toggle_pinned_label =
+      uses_essentials
+          ? (will_pin ? IDS_TAB_CXMENU_ADD_TO_ESSENTIALS
+                      : IDS_TAB_CXMENU_REMOVE_FROM_ESSENTIALS)
+          : (will_pin ? IDS_TAB_CXMENU_PIN_TAB : IDS_TAB_CXMENU_UNPIN_TAB);
+  AddItemWithStringId(TabStripModel::CommandTogglePinned, toggle_pinned_label);
 
   if (features::IsMenuSimplificationEnabled()) {
     SetIconForCommandId(
diff --git a/chrome/browser/ui/tabs/tab_menu_model_browsertest.cc b/chrome/browser/ui/tabs/tab_menu_model_browsertest.cc
index fb317ccd288dafecce4feb524413f4f3d2a24f52..f4190e667883f327f217542e37d2a8318715d0fe 100644
--- a/chrome/browser/ui/tabs/tab_menu_model_browsertest.cc
+++ b/chrome/browser/ui/tabs/tab_menu_model_browsertest.cc
@@ -4,6 +4,8 @@
 
 #include "chrome/browser/ui/tabs/tab_menu_model.h"
 
+#include <optional>
+
 #include "base/callback_list.h"
 #include "base/feature_list.h"
 #include "base/test/run_until.h"
@@ -18,15 +20,18 @@
 #include "chrome/browser/glic/test_support/glic_test_environment.h"
 #include "chrome/browser/optimization_guide/optimization_guide_keyed_service.h"
 #include "chrome/browser/optimization_guide/optimization_guide_keyed_service_factory.h"
+#include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/signin/identity_test_environment_profile_adaptor.h"
 #include "chrome/browser/sync/send_tab_to_self_sync_service_factory.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_commands.h"
 #include "chrome/browser/ui/browser_tabstrip.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
+#include "chrome/browser/ui/browser_window/public/create_browser_window.h"
 #include "chrome/browser/ui/tab_ui_helper.h"
 #include "chrome/browser/ui/tabs/existing_base_sub_menu_model.h"
 #include "chrome/browser/ui/tabs/features.h"
+#include "chrome/browser/ui/tabs/pinned_tab_service_factory.h"
 #include "chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.h"
 #include "chrome/browser/ui/tabs/split_tab_menu_model.h"
 #include "chrome/browser/ui/tabs/split_tab_metrics.h"
@@ -109,6 +114,109 @@ IN_PROC_BROWSER_TEST_F(TabMenuModelBrowserTest, Basics) {
   EXPECT_EQ(item_count, delegate_.enable_count_);
 }
 
+IN_PROC_BROWSER_TEST_F(TabMenuModelBrowserTest,
+                       RegularProfileUsesEssentialsForPersistentPins) {
+  ASSERT_TRUE(browser()->GetProfile()->IsRegularProfile());
+  TabStripModel* const tab_strip_model = browser()->GetTabStripModel();
+  tabs::TabInterface* const essential = tab_strip_model->GetActiveTab();
+  ASSERT_TRUE(essential);
+
+  TabMenuModel add_menu(&delegate_,
+                        browser()->GetFeatures().tab_menu_model_delegate(),
+                        tab_strip_model, tab_strip_model->active_index());
+  const std::optional<size_t> add_index =
+      add_menu.GetIndexOfCommandId(TabStripModel::CommandTogglePinned);
+  ASSERT_TRUE(add_index.has_value());
+  EXPECT_EQ(l10n_util::GetStringUTF16(IDS_TAB_CXMENU_ADD_TO_ESSENTIALS),
+            add_menu.GetLabelAt(add_index.value()));
+
+  tab_strip_model->ExecuteContextMenuCommand(
+      tab_strip_model->active_index(), TabStripModel::CommandTogglePinned);
+  ASSERT_TRUE(essential->IsPinned());
+
+  chrome::AddTabAt(browser(), GURL("about:blank"), tab_strip_model->count(),
+                   /*foreground=*/true);
+  const int essential_index = tab_strip_model->GetIndexOfTab(essential);
+  ASSERT_NE(TabStripModel::kNoTab, essential_index);
+
+  TabMenuModel remove_menu(&delegate_,
+                           browser()->GetFeatures().tab_menu_model_delegate(),
+                           tab_strip_model, essential_index);
+  const std::optional<size_t> remove_index =
+      remove_menu.GetIndexOfCommandId(TabStripModel::CommandTogglePinned);
+  ASSERT_TRUE(remove_index.has_value());
+  EXPECT_EQ(l10n_util::GetStringUTF16(IDS_TAB_CXMENU_REMOVE_FROM_ESSENTIALS),
+            remove_menu.GetLabelAt(remove_index.value()));
+  EXPECT_TRUE(remove_menu.GetIndexOfCommandId(TabStripModel::CommandUnloadTab)
+                  .has_value());
+  EXPECT_TRUE(tab_strip_model->IsContextMenuCommandEnabled(
+      essential_index, TabStripModel::CommandUnloadTab));
+}
+
+IN_PROC_BROWSER_TEST_F(TabMenuModelBrowserTest,
+                       RegularProfilePopupRetainsPinTerminology) {
+  Browser* const popup =
+      CreateBrowserWindow(
+          BrowserWindowCreateParams(BrowserWindowInterface::TYPE_POPUP,
+                                    browser()->GetProfile(),
+                                    /*from_user_gesture=*/true))
+          ->GetBrowserForMigrationOnly();
+  ASSERT_TRUE(popup);
+  ASSERT_EQ(popup->GetType(), BrowserWindowInterface::Type::TYPE_POPUP);
+  chrome::AddTabAt(popup, GURL("about:blank"), /*index=*/-1,
+                   /*foreground=*/true);
+
+  TabStripModel* const tab_strip_model = popup->GetTabStripModel();
+  ASSERT_EQ(1, tab_strip_model->count());
+  {
+    TabMenuModel menu(&delegate_,
+                      popup->GetFeatures().tab_menu_model_delegate(),
+                      tab_strip_model, tab_strip_model->active_index());
+    const std::optional<size_t> pin_index =
+        menu.GetIndexOfCommandId(TabStripModel::CommandTogglePinned);
+    ASSERT_TRUE(pin_index.has_value());
+    EXPECT_EQ(l10n_util::GetStringUTF16(IDS_TAB_CXMENU_PIN_TAB),
+              menu.GetLabelAt(pin_index.value()));
+  }
+
+  tab_strip_model->CloseAllTabs();
+}
+
+IN_PROC_BROWSER_TEST_F(TabMenuModelBrowserTest,
+                       OffTheRecordNormalWindowRetainsPinTerminology) {
+  Browser* const otr_browser = CreateIncognitoBrowser(browser()->GetProfile());
+  ASSERT_TRUE(otr_browser);
+  ASSERT_EQ(otr_browser->GetType(), BrowserWindowInterface::Type::TYPE_NORMAL);
+  EXPECT_FALSE(
+      PinnedTabServiceFactory::GetForProfile(otr_browser->GetProfile()));
+
+  TabStripModel* const tab_strip_model = otr_browser->GetTabStripModel();
+  tabs::TabInterface* const tab = tab_strip_model->GetActiveTab();
+  ASSERT_TRUE(tab);
+  {
+    TabMenuModel pin_menu(&delegate_,
+                          otr_browser->GetFeatures().tab_menu_model_delegate(),
+                          tab_strip_model, tab_strip_model->active_index());
+    const std::optional<size_t> pin_index =
+        pin_menu.GetIndexOfCommandId(TabStripModel::CommandTogglePinned);
+    ASSERT_TRUE(pin_index.has_value());
+    EXPECT_EQ(l10n_util::GetStringUTF16(IDS_TAB_CXMENU_PIN_TAB),
+              pin_menu.GetLabelAt(pin_index.value()));
+  }
+
+  tab_strip_model->ExecuteContextMenuCommand(
+      tab_strip_model->active_index(), TabStripModel::CommandTogglePinned);
+  ASSERT_TRUE(tab->IsPinned());
+  TabMenuModel unpin_menu(&delegate_,
+                          otr_browser->GetFeatures().tab_menu_model_delegate(),
+                          tab_strip_model, tab_strip_model->active_index());
+  const std::optional<size_t> unpin_index =
+      unpin_menu.GetIndexOfCommandId(TabStripModel::CommandTogglePinned);
+  ASSERT_TRUE(unpin_index.has_value());
+  EXPECT_EQ(l10n_util::GetStringUTF16(IDS_TAB_CXMENU_UNPIN_TAB),
+            unpin_menu.GetLabelAt(unpin_index.value()));
+}
+
 IN_PROC_BROWSER_TEST_F(TabMenuModelBrowserTest,
                        LifecycleCommandsAreHiddenForOrdinaryTab) {
   TabStripModel* const tab_strip_model = browser()->tab_strip_model();
diff --git a/chrome/browser/ui/views/tabs/common/BUILD.gn b/chrome/browser/ui/views/tabs/common/BUILD.gn
index 205cea6fe98595b7975bdcb82df33d4f4f6feb6c..16951a112d6f2d54f0da07650a2f4022ef64078d 100644
--- a/chrome/browser/ui/views/tabs/common/BUILD.gn
+++ b/chrome/browser/ui/views/tabs/common/BUILD.gn
@@ -181,8 +181,10 @@ source_set("browser_tests") {
     "//chrome/test:test_support",
     "//chrome/test:test_support_ui",
     "//content/test:test_support",
+    "//skia",
     "//testing/gmock",
     "//ui/events:test_support",
+    "//ui/views:test_support",
   ]
 
   if (use_ozone) {
diff --git a/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc b/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc
index ac9e68133224396d7a7440d49e30255ac529b723..9e32fe6d167295cf50be1d637fd1186c1eeae179 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc
@@ -44,11 +44,13 @@
 #include "content/public/test/browser_test_utils.h"
 #include "media/base/media_switches.h"
 #include "net/test/embedded_test_server/embedded_test_server.h"
+#include "third_party/skia/include/core/SkBitmap.h"
 #include "ui/base/l10n/l10n_util.h"
 #include "ui/events/base_event_utils.h"
 #include "ui/events/test/event_generator.h"
 #include "ui/views/controls/button/button_controller.h"
 #include "ui/views/controls/label.h"
+#include "ui/views/test/views_drawing_test_utils.h"
 #include "ui/views/widget/widget_utils.h"
 
 class TestWebContentsObserver : public content::WebContentsObserver {
@@ -599,6 +601,21 @@ IN_PROC_BROWSER_TEST_F(TabViewTest, PinnedTabsUseStableActiveOnlyOutline) {
   EXPECT_TRUE(first->GetBackgroundColor().has_value());
   EXPECT_TRUE(second->GetBackgroundColor().has_value());
 
+  // The inactive Essential tile must round the surface itself. Previously the
+  // inactive tile's empty border exposed a square fill while the active tile's
+  // visible rounded outline merely hid the same paint bug.
+  WaitForLayout(first);
+  const gfx::Rect contents = first->GetContentsBounds();
+  ASSERT_FALSE(contents.IsEmpty());
+  const SkBitmap inactive_bitmap = views::test::PaintViewToBitmap(first);
+  const SkColor untouched_color = inactive_bitmap.getColor(0, 0);
+  EXPECT_EQ(untouched_color,
+            inactive_bitmap.getColor(contents.x(), contents.y()));
+  const gfx::Point center = contents.CenterPoint();
+  const gfx::Point fill_sample(contents.x() + 2, center.y());
+  EXPECT_NE(untouched_color,
+            inactive_bitmap.getColor(fill_sample.x(), fill_sample.y()));
+
   tab_strip_model()->ActivateTabAt(0);
   ASSERT_TRUE(base::test::RunUntil([&]() {
     return first->ShouldPaintActivePinnedOutline() &&
diff --git a/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc b/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc
index 9c19954f20cc1985bd0f1e9f473cfc5b22c61e08..ef5942442df10e3c0176673665cde4389060a011 100644
--- a/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc
+++ b/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc
@@ -130,8 +130,14 @@ void VerticalTabStyleViews::PaintTabBackgroundFill(
     TabStyle::TabSelectionState selection_state,
     bool hovered,
     std::optional<int> fill_id) const {
-  const SkPath fill_path =
-      GetPath(TabStyle::PathType::kHighlight, canvas->image_scale(), {});
+  const views::View* const view = delegate_->GetView();
+  const float image_scale = canvas->image_scale();
+  gfx::RectF fill_bounds(view->GetContentsBounds());
+  fill_bounds.Scale(image_scale);
+  const SkScalar corner_radius =
+      (GetCornerRadius() - view->GetInsets().top() / 2.0f) * image_scale;
+  const SkPath fill_path = SkPath::RRect(SkRRect::MakeRectXY(
+      gfx::RectFToSkRect(fill_bounds), corner_radius, corner_radius));
 
   gfx::ScopedCanvas scoped_canvas(canvas);
   const float scale = canvas->UndoDeviceScaleFactor();
