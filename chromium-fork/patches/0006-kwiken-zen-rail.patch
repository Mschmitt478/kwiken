diff --git a/chrome/browser/ui/color/chrome_color_mixers.cc b/chrome/browser/ui/color/chrome_color_mixers.cc
index 8131056e212956decf4831730d1ab513a6c6afa4..912072c8e1434a010c4dd9f3caebc0e3866197ae 100644
--- a/chrome/browser/ui/color/chrome_color_mixers.cc
+++ b/chrome/browser/ui/color/chrome_color_mixers.cc
@@ -44,27 +44,63 @@ void AddKwikenColorMixer(ui::ColorProvider* provider,
       key.color_mode == ui::ColorProviderKey::ColorMode::kDark &&
       key.user_color_source ==
           ui::ColorProviderKey::UserColorSource::kGrayscale;
-  const SkColor sidebar = private_palette ? SkColorSetRGB(0x17, 0x13, 0x1D)
-                                          : SkColorSetRGB(0x14, 0x18, 0x11);
+  const SkColor sidebar = private_palette ? SkColorSetRGB(0x2A, 0x20, 0x2F)
+                                          : SkColorSetRGB(0x7D, 0x68, 0x82);
   const SkColor sidebar_raised = private_palette
-                                     ? SkColorSetRGB(0x22, 0x1B, 0x2B)
-                                     : SkColorSetRGB(0x1D, 0x23, 0x19);
-  const SkColor accent = private_palette ? SkColorSetRGB(0xC9, 0xAD, 0xFF)
-                                         : SkColorSetRGB(0xC8, 0xFF, 0x5A);
-  const SkColor accent_strong = private_palette
-                                    ? SkColorSetRGB(0xAD, 0x8C, 0xF1)
-                                    : SkColorSetRGB(0xAE, 0xE6, 0x3A);
-  constexpr SkColor text = SkColorSetRGB(0xF2, 0xF5, 0xED);
-  constexpr SkColor muted = SkColorSetRGB(0x8D, 0x96, 0x86);
-  constexpr SkColor paper = SkColorSetRGB(0xF6, 0xF7, 0xF3);
-  constexpr SkColor paper_raised = SK_ColorWHITE;
-  constexpr SkColor paper_text = SkColorSetRGB(0x25, 0x2B, 0x21);
-  constexpr SkColor paper_muted = SkColorSetRGB(0x73, 0x7B, 0x6E);
-  constexpr SkColor paper_line = SkColorSetRGB(0xE2, 0xE5, 0xDE);
+                                     ? SkColorSetRGB(0x38, 0x2A, 0x3E)
+                                     : SkColorSetRGB(0x75, 0x61, 0x7A);
+  const SkColor sidebar_hover = private_palette
+                                    ? SkColorSetRGB(0x4A, 0x38, 0x51)
+                                    : SkColorSetRGB(0x8A, 0x74, 0x8F);
+  const SkColor active_tile = private_palette ? SkColorSetRGB(0xD2, 0xC4, 0xD4)
+                                              : SkColorSetRGB(0xDC, 0xD6, 0xDD);
+  const SkColor active_foreground = private_palette
+                                        ? SkColorSetRGB(0x24, 0x1C, 0x27)
+                                        : SkColorSetRGB(0x2D, 0x26, 0x30);
+  const SkColor text = private_palette ? SkColorSetRGB(0xF5, 0xED, 0xF6)
+                                       : SkColorSetRGB(0xF1, 0xEB, 0xF2);
+  const SkColor muted = private_palette ? SkColorSetRGB(0xC6, 0xB7, 0xC9)
+                                        : SkColorSetRGB(0xD0, 0xC4, 0xD2);
+  const SkColor accent = private_palette ? SkColorSetRGB(0xD7, 0xAF, 0xC1)
+                                         : SkColorSetRGB(0xE5, 0xC6, 0xD1);
+  const SkColor paper = private_palette ? SkColorSetRGB(0x1B, 0x15, 0x1E)
+                                        : SkColorSetRGB(0xF7, 0xF3, 0xF6);
+  const SkColor paper_raised = private_palette
+                                   ? SkColorSetRGB(0x26, 0x1D, 0x2A)
+                                   : SkColorSetRGB(0xFF, 0xF9, 0xFD);
+  const SkColor paper_text =
+      private_palette ? SkColorSetRGB(0xF4, 0xED, 0xF4) : active_foreground;
+  const SkColor paper_muted = private_palette ? SkColorSetRGB(0xC3, 0xB5, 0xC5)
+                                              : SkColorSetRGB(0x76, 0x6C, 0x78);
+  const SkColor paper_line = private_palette ? SkColorSetRGB(0x46, 0x38, 0x4A)
+                                             : SkColorSetRGB(0xE5, 0xDD, 0xE4);
+
+  // Keep every group hue distinct while lowering saturation against the rail.
+  const SkColor group_grey = private_palette ? SkColorSetRGB(0x55, 0x49, 0x59)
+                                             : SkColorSetRGB(0x8E, 0x7F, 0x91);
+  const SkColor group_blue = private_palette ? SkColorSetRGB(0x45, 0x50, 0x68)
+                                             : SkColorSetRGB(0x7B, 0x87, 0xA6);
+  const SkColor group_red = private_palette ? SkColorSetRGB(0x67, 0x43, 0x4B)
+                                            : SkColorSetRGB(0xA7, 0x79, 0x82);
+  const SkColor group_yellow = private_palette
+                                   ? SkColorSetRGB(0x68, 0x5A, 0x3E)
+                                   : SkColorSetRGB(0xA7, 0x95, 0x6D);
+  const SkColor group_green = private_palette ? SkColorSetRGB(0x43, 0x5B, 0x48)
+                                              : SkColorSetRGB(0x7E, 0x9A, 0x82);
+  const SkColor group_pink = private_palette ? SkColorSetRGB(0x66, 0x45, 0x59)
+                                             : SkColorSetRGB(0xA7, 0x7E, 0x99);
+  const SkColor group_purple = private_palette
+                                   ? SkColorSetRGB(0x59, 0x48, 0x68)
+                                   : SkColorSetRGB(0x92, 0x7D, 0xA6);
+  const SkColor group_cyan = private_palette ? SkColorSetRGB(0x3D, 0x5B, 0x5E)
+                                             : SkColorSetRGB(0x72, 0x9A, 0x9D);
+  const SkColor group_orange = private_palette
+                                   ? SkColorSetRGB(0x66, 0x4E, 0x3F)
+                                   : SkColorSetRGB(0xA7, 0x86, 0x70);
 
   ui::ColorMixer& mixer = provider->AddMixer();
-  mixer[ui::kColorAccent] = {accent_strong};
-  mixer[ui::kColorFocusableBorderFocused] = {accent_strong};
+  mixer[ui::kColorAccent] = {accent};
+  mixer[ui::kColorFocusableBorderFocused] = {accent};
   mixer[ui::kColorFrameActive] = {sidebar};
   mixer[ui::kColorFrameInactive] = {sidebar_raised};
   mixer[ui::kColorSysHeader] = {sidebar};
@@ -75,38 +111,56 @@ void AddKwikenColorMixer(ui::ColorProvider* provider,
       SkColorSetARGB(0x20, 0xFF, 0xFF, 0xFF)};
   mixer[ui::kColorSysOnHeaderDividerInactive] = {
       SkColorSetARGB(0x18, 0xFF, 0xFF, 0xFF)};
-  mixer[ui::kColorSysStateHeaderHover] = {
-      SkColorSetARGB(0x12, 0xFF, 0xFF, 0xFF)};
-  mixer[ui::kColorSysStateHeaderHoverInactive] = {
-      SkColorSetARGB(0x0D, 0xFF, 0xFF, 0xFF)};
-
-  mixer[kColorTabBackgroundActiveFrameActive] = {sidebar_raised};
-  mixer[kColorTabBackgroundActiveFrameInactive] = {sidebar_raised};
-  mixer[kColorTabBackgroundInactiveFrameActive] = {sidebar};
+  mixer[ui::kColorSysStateHeaderHover] = {sidebar_hover};
+  mixer[ui::kColorSysStateHeaderHoverInactive] = {sidebar_hover};
+
+  mixer[kColorTabBackgroundActiveFrameActive] = {active_tile};
+  mixer[kColorTabBackgroundActiveFrameInactive] = {active_tile};
+  mixer[kColorTabBackgroundInactiveFrameActive] = {sidebar_raised};
   mixer[kColorTabBackgroundInactiveFrameInactive] = {sidebar_raised};
-  mixer[kColorTabBackgroundInactiveHoverFrameActive] = {sidebar_raised};
-  mixer[kColorTabBackgroundInactiveHoverFrameInactive] = {sidebar_raised};
-  mixer[kColorTabBackgroundSelectedFrameActive] = {sidebar_raised};
-  mixer[kColorTabBackgroundSelectedFrameInactive] = {sidebar_raised};
-  mixer[kColorTabBackgroundSelectedHoverFrameActive] = {sidebar_raised};
-  mixer[kColorTabBackgroundSelectedHoverFrameInactive] = {sidebar_raised};
-  mixer[kColorTabForegroundActiveFrameActive] = {text};
-  mixer[kColorTabForegroundActiveFrameInactive] = {text};
-  mixer[kColorTabForegroundInactiveFrameActive] = {muted};
-  mixer[kColorTabForegroundInactiveFrameInactive] = {muted};
+  mixer[kColorTabBackgroundInactiveHoverFrameActive] = {sidebar_hover};
+  mixer[kColorTabBackgroundInactiveHoverFrameInactive] = {sidebar_hover};
+  mixer[kColorTabBackgroundSelectedFrameActive] = {sidebar_hover};
+  mixer[kColorTabBackgroundSelectedFrameInactive] = {sidebar_hover};
+  mixer[kColorTabBackgroundSelectedHoverFrameActive] = {sidebar_hover};
+  mixer[kColorTabBackgroundSelectedHoverFrameInactive] = {sidebar_hover};
+  mixer[kColorTabForegroundActiveFrameActive] = {active_foreground};
+  mixer[kColorTabForegroundActiveFrameInactive] = {active_foreground};
+  mixer[kColorTabForegroundInactiveFrameActive] = {text};
+  mixer[kColorTabForegroundInactiveFrameInactive] = {text};
   mixer[kColorTabDividerFrameActive] = {SkColorSetARGB(0x20, 0xFF, 0xFF, 0xFF)};
   mixer[kColorTabDividerFrameInactive] = {
       SkColorSetARGB(0x18, 0xFF, 0xFF, 0xFF)};
-  mixer[kColorVerticalTabPinnedOutline] = {accent_strong};
-  mixer[kColorNewTabButtonForegroundFrameActive] = {accent};
-  mixer[kColorNewTabButtonForegroundFrameInactive] = {accent};
+  mixer[kColorVerticalTabPinnedOutline] = {accent};
+
+  mixer[kColorTabGroupTabStripFrameActiveGrey] = {group_grey};
+  mixer[kColorTabGroupTabStripFrameInactiveGrey] = {group_grey};
+  mixer[kColorTabGroupTabStripFrameActiveBlue] = {group_blue};
+  mixer[kColorTabGroupTabStripFrameInactiveBlue] = {group_blue};
+  mixer[kColorTabGroupTabStripFrameActiveRed] = {group_red};
+  mixer[kColorTabGroupTabStripFrameInactiveRed] = {group_red};
+  mixer[kColorTabGroupTabStripFrameActiveYellow] = {group_yellow};
+  mixer[kColorTabGroupTabStripFrameInactiveYellow] = {group_yellow};
+  mixer[kColorTabGroupTabStripFrameActiveGreen] = {group_green};
+  mixer[kColorTabGroupTabStripFrameInactiveGreen] = {group_green};
+  mixer[kColorTabGroupTabStripFrameActivePink] = {group_pink};
+  mixer[kColorTabGroupTabStripFrameInactivePink] = {group_pink};
+  mixer[kColorTabGroupTabStripFrameActivePurple] = {group_purple};
+  mixer[kColorTabGroupTabStripFrameInactivePurple] = {group_purple};
+  mixer[kColorTabGroupTabStripFrameActiveCyan] = {group_cyan};
+  mixer[kColorTabGroupTabStripFrameInactiveCyan] = {group_cyan};
+  mixer[kColorTabGroupTabStripFrameActiveOrange] = {group_orange};
+  mixer[kColorTabGroupTabStripFrameInactiveOrange] = {group_orange};
+
+  mixer[kColorNewTabButtonForegroundFrameActive] = {text};
+  mixer[kColorNewTabButtonForegroundFrameInactive] = {text};
   mixer[kColorNewTabButtonBackgroundFrameActive] = {sidebar_raised};
   mixer[kColorNewTabButtonBackgroundFrameInactive] = {sidebar_raised};
-  mixer[kColorNewTabButtonCRForegroundFrameActive] = {accent};
-  mixer[kColorNewTabButtonCRForegroundFrameInactive] = {accent};
+  mixer[kColorNewTabButtonCRForegroundFrameActive] = {text};
+  mixer[kColorNewTabButtonCRForegroundFrameInactive] = {text};
   mixer[kColorNewTabButtonCRBackgroundFrameActive] = {sidebar_raised};
   mixer[kColorNewTabButtonCRBackgroundFrameInactive] = {sidebar_raised};
-  mixer[kColorNewTabButtonFocusRing] = {accent_strong};
+  mixer[kColorNewTabButtonFocusRing] = {accent};
 
   mixer[kColorToolbar] = {paper};
   mixer[kColorToolbarBackgroundSubtleEmphasis] = {paper_raised};
@@ -137,9 +191,9 @@ void AddKwikenColorMixer(ui::ColorProvider* provider,
   mixer[kColorOmniboxTextDimmed] = {paper_muted};
   mixer[kColorOmniboxResultsTextSelected] = {paper_text};
   mixer[kColorOmniboxResultsTextSecondary] = {paper_muted};
-  mixer[kColorOmniboxResultsUrl] = {accent_strong};
-  mixer[kColorOmniboxResultsUrlSelected] = {accent_strong};
-  mixer[kColorOmniboxResultsFocusIndicator] = {accent_strong};
+  mixer[kColorOmniboxResultsUrl] = {accent};
+  mixer[kColorOmniboxResultsUrlSelected] = {accent};
+  mixer[kColorOmniboxResultsFocusIndicator] = {accent};
 
   mixer[kColorNewTabPageBackground] = {paper};
   mixer[kColorNewTabPageBackgroundOverride] = {paper};
@@ -147,12 +201,12 @@ void AddKwikenColorMixer(ui::ColorProvider* provider,
   mixer[kColorNewTabPageTextUnthemed] = {paper_text};
   mixer[kColorNewTabPagePrimaryForeground] = {paper_text};
   mixer[kColorNewTabPageSecondaryForeground] = {paper_muted};
-  mixer[kColorNewTabPageLink] = {accent_strong};
-  mixer[kColorNewTabPageLogo] = {accent_strong};
+  mixer[kColorNewTabPageLink] = {accent};
+  mixer[kColorNewTabPageLogo] = {accent};
   mixer[kColorNewTabPageSearchBoxBackground] = {paper_raised};
   mixer[kColorNewTabPageSearchBoxBackgroundHovered] = {paper_raised};
   mixer[kColorNewTabPageMostVisitedTileBackground] = {paper_raised};
-  mixer[kColorNewTabPageFocusRing] = {accent_strong};
+  mixer[kColorNewTabPageFocusRing] = {accent};
 }
 
 class ChromeColorProviderUtilsCallbacks
diff --git a/chrome/browser/ui/layout_constants.cc b/chrome/browser/ui/layout_constants.cc
index a8b7ef68008b132ec786b91ddcb232c16a0c5750..fc7036ebc0b4bf782b7d9106d4cd781f7ab182a5 100644
--- a/chrome/browser/ui/layout_constants.cc
+++ b/chrome/browser/ui/layout_constants.cc
@@ -145,15 +145,17 @@ int GetLayoutConstant(LayoutConstant constant) {
       return 20;
     case LayoutConstant::kMainBackgroundRegionCornerRadius:
     case LayoutConstant::kToolbarCornerRadius:
+      return 8;
     case LayoutConstant::kVerticalTabCornerRadius:
       return 8;
     case LayoutConstant::kVerticalTabHeight:
       return 30;
     case LayoutConstant::kVerticalTabPinnedHeight:
-      return 32;
+      return 44;
     case LayoutConstant::kVerticalTabMinWidth:
       return 32;
     case LayoutConstant::kVerticalTabStripHorizontalPadding:
+      // Keep Chromium's 12 DIP rail inset around the denser pinned grid.
       return 12;
     case LayoutConstant::kVerticalTabStripUncollapsedVerticalPadding:
       return 12;
@@ -176,7 +178,7 @@ int GetLayoutConstant(LayoutConstant constant) {
     case LayoutConstant::kVerticalTabStripTopContainerButtonSize:
       return 28;
     case LayoutConstant::kVerticalTabPinnedBorderThickness:
-      return 1;
+      return 2;
     default:
       break;
   }
diff --git a/chrome/browser/ui/tab_ui_helper.cc b/chrome/browser/ui/tab_ui_helper.cc
index 17dc6e180456b614c57cd034c34391efbba537d4..7148c619d018c2f710b0bc67f5a2ca0df6dba3a3 100644
--- a/chrome/browser/ui/tab_ui_helper.cc
+++ b/chrome/browser/ui/tab_ui_helper.cc
@@ -283,10 +283,12 @@ void TabUIHelper::OnVisibilityChanged(content::Visibility visiblity) {
 }
 
 void TabUIHelper::WasDiscarded() {
-  // Notify observers that the tab should update its UI to show discard status.
-  if (ShouldShowDiscardStatus()) {
-    tab_ui_change_callbacks_.Notify();
-  }
+  // `ShouldShowDiscardStatus()` intentionally filters Chromium's automatic
+  // memory-saver messaging by reason. Kwiken also has an explicit EXTERNAL
+  // unload command, and session-restored tabs can already be unloaded, so let
+  // TabData promote the underlying discarded state into the shared visual and
+  // accessibility path.
+  tab_ui_change_callbacks_.Notify();
 }
 
 void TabUIHelper::DidFinishNavigation(
diff --git a/chrome/browser/ui/tabs/tab_data.cc b/chrome/browser/ui/tabs/tab_data.cc
index 5056be27f3b1fec30597ecbf8eb51fdbc5765dba..1f01557a58f63c1f7aefbba2fd5176935b98ef41 100644
--- a/chrome/browser/ui/tabs/tab_data.cc
+++ b/chrome/browser/ui/tabs/tab_data.cc
@@ -46,12 +46,12 @@ TabData TabData::FromTabInterface(tabs::TabInterface* tab_interface) {
   tab_data.should_display_url = tab_ui_helper->ShouldDisplayURL();
   tab_data.visible_url = tab_ui_helper->GetVisibleURL();
   tab_data.needs_attention = tab_ui_helper->needs_attention();
+  tab_data.is_tab_discarded = tab_ui_helper->IsDiscarded();
   tab_data.should_show_discard_status =
-      tab_ui_helper->ShouldShowDiscardStatus();
+      tab_ui_helper->ShouldShowDiscardStatus() || tab_data.is_tab_discarded;
   tab_data.discarded_memory_savings =
       tab_ui_helper->GetDiscardedMemorySavings();
   tab_data.network_state = tab_ui_helper->GetTabNetworkState();
-  tab_data.is_tab_discarded = tab_ui_helper->IsDiscarded();
   tab_data.last_committed_url = tab_ui_helper->GetLastCommittedURL();
   tab_data.alert_state = tab_alert_controller->GetAlertToShow();
   tab_data.pinned = tab_interface->IsPinned();
diff --git a/chrome/browser/ui/tabs/tab_data_browsertest.cc b/chrome/browser/ui/tabs/tab_data_browsertest.cc
index bba72e28b519dadad4ad6bbef215f4bea9cfe3be..c4c3b61ffc24f4b8c4577435c07ac9c97aacf5f2 100644
--- a/chrome/browser/ui/tabs/tab_data_browsertest.cc
+++ b/chrome/browser/ui/tabs/tab_data_browsertest.cc
@@ -400,6 +400,20 @@ IN_PROC_BROWSER_TEST_F(TabDataObserverBrowserTest, TabLifecycleManagement) {
   ASSERT_TRUE(data_usage.tab_resource_usage);
   EXPECT_EQ(data_usage.tab_resource_usage->memory_usage(),
             base::ByteSize(1234));
+
+  // A session-restored tab can already be unloaded without a Chromium
+  // memory-saver discard reason. The underlying discarded bit must still feed
+  // the common icon, hover-card and accessibility status path.
+  content::WebContents* const web_contents = tab_interface->GetContents();
+  const std::u16string original_title = data_usage.title;
+  const GURL original_url = data_usage.last_committed_url;
+  web_contents->SetWasDiscarded(true);
+  tabs::TabData data_restored_unloaded =
+      tabs::TabData::FromTabInterface(tab_interface);
+  EXPECT_TRUE(data_restored_unloaded.is_tab_discarded);
+  EXPECT_TRUE(data_restored_unloaded.should_show_discard_status);
+  EXPECT_EQ(original_title, data_restored_unloaded.title);
+  EXPECT_EQ(original_url, data_restored_unloaded.last_committed_url);
 }
 
 IN_PROC_BROWSER_TEST_F(TabDataObserverBrowserTest,
diff --git a/chrome/browser/ui/views/tabs/common/BUILD.gn b/chrome/browser/ui/views/tabs/common/BUILD.gn
index 9354e0d5497b85db204ebae5d6adcd41973ea01a..205cea6fe98595b7975bdcb82df33d4f4f6feb6c 100644
--- a/chrome/browser/ui/views/tabs/common/BUILD.gn
+++ b/chrome/browser/ui/views/tabs/common/BUILD.gn
@@ -171,11 +171,13 @@ source_set("browser_tests") {
     "//chrome/browser/ui:ui_features",
     "//chrome/browser/ui/animation",
     "//chrome/browser/ui/browser_window",
+    "//chrome/browser/ui/color:color_headers",
     "//chrome/browser/ui/tabs/tab_strip_api",
     "//chrome/browser/ui/views/animations",
     "//chrome/browser/ui/views/tabs/hovercard:hovercard_helpers",
     "//chrome/browser/ui/views/tabs/shared",
     "//chrome/browser/ui/views/tabs/tab",
+    "//chrome/browser/ui/window_metadata",
     "//chrome/test:test_support",
     "//chrome/test:test_support_ui",
     "//content/test:test_support",
diff --git a/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc b/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
index af4cd825d4c10c6b0802cf4134a902dd7346e9b8..f4c05e0d7d3ff8958abb07a016d35fd2a56fb1a5 100644
--- a/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
+++ b/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
@@ -26,7 +26,8 @@
 #include "ui/views/view_utils.h"
 
 namespace {
-constexpr int kTabPadding = 4;
+constexpr int kExpandedPinnedTabMinWidth = 64;
+constexpr int kPinnedTabGridGap = 8;
 }  // namespace
 
 PinnedTabContainerView::PinnedTabContainerView(
@@ -121,11 +122,15 @@ gfx::Size PinnedTabContainerView::GetMinimumSize() const {
                                                                         : 1.0f),
       static_cast<float>(num_children));
   const int min_height =
-      base::ClampCeil(GetLayoutConstant(LayoutConstant::kVerticalTabHeight) *
-                      min_rows) +
-      (num_children > 1 ? kTabPadding : 0);
-  return gfx::Size(GetLayoutConstant(LayoutConstant::kVerticalTabMinWidth),
-                   min_height);
+      base::ClampCeil(
+          GetLayoutConstant(LayoutConstant::kVerticalTabPinnedHeight) *
+          min_rows) +
+      (num_children > 1 ? kPinnedTabGridGap : 0);
+  const int min_width =
+      collapse_state == tabs::VerticalTabStripCollapseState::kExpanded
+          ? kExpandedPinnedTabMinWidth
+          : GetLayoutConstant(LayoutConstant::kVerticalTabMinWidth);
+  return gfx::Size(min_width, min_height);
 }
 
 bool PinnedTabContainerView::IsDragging() const {
@@ -185,7 +190,7 @@ PinnedTabContainerView::GetLinkDropIndexForHorizontal(
     const gfx::Point& loc_in_container) {
   const bool is_vertical =
       collection_node_->orientation() == TabStripOrientation::kVertical;
-  const int padding = is_vertical ? kTabPadding : 0;
+  const int padding = is_vertical ? kPinnedTabGridGap : 0;
   const int logical_x = GetMirroredXInView(loc_in_container.x());
 
   for (auto& child_node : collection_node_->children()) {
@@ -386,45 +391,46 @@ views::ProposedLayout PinnedTabContainerView::CalculateVerticalLayout(
     return layouts;
   }
 
-  // Child width will be uniform and match the largest child's width.
-  bool contains_split = false;
-  for (const auto& i : collection_node_->children()) {
-    if (i->type() == TabCollectionNode::Type::SPLIT) {
-      contains_split = true;
-    }
-  }
-  int child_width = GetLayoutConstant(LayoutConstant::kVerticalTabMinWidth) *
-                    (contains_split ? 2 : 1);
+  const auto collapse_state = GetTabStripCollapseState();
+  const bool is_expanded =
+      collapse_state == tabs::VerticalTabStripCollapseState::kExpanded;
+  const bool is_collapsed =
+      tabs::IsVerticalTabsExpandOnHoverFeatureEnabled() &&
+      collapse_state == tabs::VerticalTabStripCollapseState::kCollapsed;
+  const int minimum_child_width =
+      is_expanded ? kExpandedPinnedTabMinWidth
+                  : GetLayoutConstant(LayoutConstant::kVerticalTabMinWidth);
+  int child_width = minimum_child_width;
 
   // If the width is bounded, calculate how many children can fit on a row.
   // Since all children are allocated the same width this will be the same for
   // every row.
   if (size_bounds.width().is_bounded() && size_bounds.width().value() > 0) {
-    auto collapse_state = GetTabStripCollapseState();
-
     // Apply horizontal padding immediately at start of collapse animation by
     // including collapsing state.
-    int available_width =
-        size_bounds.width().value() -
-        GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding);
+    const int available_width =
+        std::max(minimum_child_width,
+                 size_bounds.width().value() -
+                     GetLayoutConstant(
+                         LayoutConstant::kVerticalTabStripHorizontalPadding));
 
     // When we are in collapsed state, only one child should be shown per row.
     // During collapse animation and other cases, fit as many as possible.
     children_on_row =
-        tabs::IsVerticalTabsExpandOnHoverFeatureEnabled() &&
-                collapse_state ==
-                    tabs::VerticalTabStripCollapseState::kCollapsed
+        is_collapsed
             ? 1
             : std::min(
                   children_on_row,
-                  static_cast<int>(std::floor((available_width - child_width) /
-                                              (child_width + kTabPadding)) +
-                                   1));
-
-    // Allocate extra space to the tabs.
-    available_width -=
-        (children_on_row * child_width) + (kTabPadding * (children_on_row - 1));
-    child_width += std::floor(available_width / children_on_row);
+                  std::max(1, (available_width + kPinnedTabGridGap) /
+                                  (minimum_child_width + kPinnedTabGridGap)));
+
+    // Allocate remaining width evenly without shrinking below the expanded
+    // 64 DIP contract or clipping the trailing tile.
+    child_width =
+        std::max(minimum_child_width,
+                 (available_width -
+                  kPinnedTabGridGap * static_cast<int>(children_on_row - 1)) /
+                     children_on_row);
   }
 
   int row_index = 0;
@@ -448,12 +454,12 @@ views::ProposedLayout PinnedTabContainerView::CalculateVerticalLayout(
     bounds.set_x(child_x);
 
     if (row_index != 0) {
-      bounds.set_x(bounds.x() + kTabPadding);
+      bounds.set_x(bounds.x() + kPinnedTabGridGap);
     }
 
     if (!drag_data || !drag_data->should_float) {
       if (row_index != 0) {
-        x += kTabPadding;
+        x += kPinnedTabGridGap;
       }
       x += bounds.width();
       total_width = std::max(total_width, x);
@@ -461,7 +467,7 @@ views::ProposedLayout PinnedTabContainerView::CalculateVerticalLayout(
 
       row_index++;
       if (row_index >= children_on_row) {
-        y = total_height + kTabPadding;
+        y = total_height + kPinnedTabGridGap;
         row_index = 0;
         x = 0;
       }
diff --git a/chrome/browser/ui/views/tabs/common/split_tab_view.cc b/chrome/browser/ui/views/tabs/common/split_tab_view.cc
index 440a3076e5f80c6b393dba0b311ba15ea808681e..c65ab3ed4cb7265a93fecc90f6d78e97d0be56ba 100644
--- a/chrome/browser/ui/views/tabs/common/split_tab_view.cc
+++ b/chrome/browser/ui/views/tabs/common/split_tab_view.cc
@@ -97,10 +97,18 @@ void SplitTabView::OnPaint(gfx::Canvas* canvas) {
     const std::vector<views::View*> children =
         collection_node_ ? collection_node_->GetDirectChildren()
                          : std::vector<views::View*>();
+    TabView* background_tab = nullptr;
+    for (views::View* child : children) {
+      auto* tab = views::AsViewClass<TabView>(child);
+      if (!background_tab || tab->ShouldPaintActivePinnedOutline()) {
+        background_tab = tab;
+      }
+      if (tab->ShouldPaintActivePinnedOutline()) {
+        break;
+      }
+    }
     std::optional<SkColor> background_color =
-        !children.empty()
-            ? views::AsViewClass<TabView>(children[0])->GetBackgroundColor()
-            : std::nullopt;
+        background_tab ? background_tab->GetBackgroundColor() : std::nullopt;
     if (background_color.has_value()) {
       cc::PaintFlags flags;
       flags.setAntiAlias(true);
@@ -200,7 +208,31 @@ void SplitTabView::OnDataChanged() {
 }
 
 void SplitTabView::UpdateBorder() {
-  if (pinned_) {
+  const bool is_vertical =
+      collection_node_ &&
+      collection_node_->orientation() == TabStripOrientation::kVertical;
+  if (pinned_ && is_vertical) {
+    const int border_thickness =
+        GetLayoutConstant(LayoutConstant::kVerticalTabPinnedBorderThickness);
+    bool has_active_tab = false;
+    for (views::View* child : collection_node_->GetDirectChildren()) {
+      if (views::AsViewClass<TabView>(child)
+              ->ShouldPaintActivePinnedOutline()) {
+        has_active_tab = true;
+        break;
+      }
+    }
+
+    if (has_active_tab) {
+      SetBorder(views::CreateRoundedRectBorder(
+          border_thickness,
+          GetLayoutConstant(LayoutConstant::kVerticalTabCornerRadius),
+          kColorVerticalTabPinnedOutline));
+    } else {
+      SetBorder(views::CreateEmptyBorder(gfx::Insets(border_thickness)));
+    }
+    SchedulePaint();
+  } else if (pinned_) {
     const bool is_frame_active =
         GetWidget() ? GetWidget()->ShouldPaintAsActive() : true;
     SetBorder(views::CreateRoundedRectBorder(
diff --git a/chrome/browser/ui/views/tabs/common/split_tab_view.h b/chrome/browser/ui/views/tabs/common/split_tab_view.h
index f380ec9050e75093b4380547314103f545fc6a4b..e42bf1dd54638a9fb89cff32d614e22bbf2db95e 100644
--- a/chrome/browser/ui/views/tabs/common/split_tab_view.h
+++ b/chrome/browser/ui/views/tabs/common/split_tab_view.h
@@ -60,10 +60,13 @@ class SplitTabView : public views::View, public views::LayoutDelegate {
 
   const TabCollectionNode* collection_node() const { return collection_node_; }
 
+  // Called when either child changes activation state so split-pinned tabs use
+  // one coherent, stable outer outline.
+  void UpdateBorder();
+
  private:
   void ResetCollectionNode();
   void OnDataChanged();
-  void UpdateBorder();
   void UpdateHovered(bool hovered);
 
   views::ProposedLayout CalculateHorizontalLayout(
diff --git a/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc b/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc
index cfaf18f54f721f3f4bf0eb438454e3e41f963728..5b6539136663dd9b638e3bcaa201f55d15b4fd12 100644
--- a/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc
@@ -9,6 +9,7 @@
 #include "build/buildflag.h"
 #include "chrome/app/vector_icons/vector_icons.h"
 #include "chrome/browser/ui/browser_element_identifiers.h"
+#include "chrome/browser/ui/color/chrome_color_id.h"
 #include "chrome/browser/ui/tabs/tab_group_data.h"
 #include "chrome/browser/ui/tabs/tab_group_theme.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
@@ -52,6 +53,7 @@ constexpr int kGroupHeaderHorizontalInset = 8;
 constexpr int kIconSize = 16;
 constexpr int kFocusRingInset = 2;
 constexpr int kAttentionIndicatorWidth = 8;
+constexpr int kFolderIconLabelPadding = 8;
 // The amount of padding between the label and any sync icon.
 constexpr int kSyncIconLabelPadding = 2;
 
@@ -137,6 +139,9 @@ TabGroupHeaderView::TabGroupHeaderView(
     const tab_groups::TabGroupVisualData* tab_group_visual_data)
     : HoverCardAnchorTarget(this),
       tab_group_visual_data_(*tab_group_visual_data),
+      folder_icon_(orientation == TabStripOrientation::kVertical
+                       ? AddChildView(std::make_unique<views::ImageView>())
+                       : nullptr),
       sync_icon_(AddChildView(std::make_unique<views::ImageView>())),
       group_header_label_(
           AddChildView(std::make_unique<TabGroupHeaderLabel>())),
@@ -204,6 +209,11 @@ TabGroupHeaderView::TabGroupHeaderView(
 
   sync_icon_->SetProperty(views::kMarginsKey,
                           gfx::Insets::TLBR(0, 0, 0, kSyncIconLabelPadding));
+  if (folder_icon_) {
+    folder_icon_->SetProperty(
+        views::kMarginsKey,
+        gfx::Insets::TLBR(0, 0, 0, kFolderIconLabelPadding));
+  }
   attention_indicator_->SetProperty(
       views::kMarginsKey, gfx::Insets::TLBR(0, kSyncIconLabelPadding, 0, 0));
 
@@ -523,10 +533,27 @@ void TabGroupHeaderView::OnDataChanged(
 
   group_header_label_->SetText(tab_group_visual_data_.title());
   if (GetColorProvider()) {
-    SkColor background_color = GetColorProvider()->GetColor(
-        GetTabGroupTabStripColorId(tab_group_visual_data_.color(),
-                                   GetWidget()->ShouldPaintAsActive()));
-    SkColor foreground_color = GetForegroundColor();
+    const bool is_frame_active = GetWidget()->ShouldPaintAsActive();
+    const SkColor group_color =
+        GetColorProvider()->GetColor(GetTabGroupTabStripColorId(
+            tab_group_visual_data_.color(), is_frame_active));
+    const ui::ColorId background_color =
+        orientation_ == TabStripOrientation::kVertical
+            ? (is_frame_active ? kColorTabBackgroundInactiveFrameActive
+                               : kColorTabBackgroundInactiveFrameInactive)
+            : GetTabGroupTabStripColorId(tab_group_visual_data_.color(),
+                                         is_frame_active);
+    const SkColor foreground_color = GetForegroundColor();
+
+    // Treat a vertical group as a calm folder row. The selected hue remains
+    // visible in the folder glyph without filling the entire header.
+    if (folder_icon_) {
+      folder_icon_->SetImage(ui::ImageModel::FromVectorIcon(
+          features::IsRoundedIconsEnabled()
+              ? vector_icons::kFolderFlippableIcon
+              : vector_icons::kFolderChromeRefreshOldIcon,
+          group_color, kIconSize));
+    }
 
     // Update label.
     group_header_label_->SetEnabledColor(foreground_color);
@@ -673,6 +700,12 @@ void TabGroupHeaderView::UpdateAccessibleName() {
 
 SkColor TabGroupHeaderView::GetForegroundColor() const {
   if (GetColorProvider()) {
+    if (orientation_ == TabStripOrientation::kVertical) {
+      return GetColorProvider()->GetColor(
+          GetWidget()->ShouldPaintAsActive()
+              ? kColorTabForegroundInactiveFrameActive
+              : kColorTabForegroundInactiveFrameInactive);
+    }
     SkColor background_color = GetColorProvider()->GetColor(
         GetTabGroupTabStripColorId(tab_group_visual_data_.color(),
                                    GetWidget()->ShouldPaintAsActive()));
diff --git a/chrome/browser/ui/views/tabs/common/tab_group_header_view.h b/chrome/browser/ui/views/tabs/common/tab_group_header_view.h
index 982370f7cdb149a28adf1d5daaf2489741fb2618..d1ec10e79957a133adb8ee6181d685d1bed90027 100644
--- a/chrome/browser/ui/views/tabs/common/tab_group_header_view.h
+++ b/chrome/browser/ui/views/tabs/common/tab_group_header_view.h
@@ -33,7 +33,8 @@ class ImageView;
 class Label;
 }  // namespace views
 
-// The view for the tab group header. It displays the tab group
+// The view for the tab group header. In vertical mode it displays a folder
+// glyph in addition to the tab group
 // title, editor icon and the collapsed/expand icon.
 class TabGroupHeaderView : public views::FlexLayoutView,
                            public views::ContextMenuController,
@@ -116,6 +117,7 @@ class TabGroupHeaderView : public views::FlexLayoutView,
   tab_groups::TabGroupId group() const;
 
   views::LabelButton* editor_bubble_button() { return editor_bubble_button_; }
+  views::ImageView* folder_icon_for_testing() { return folder_icon_; }
   views::ImageView* collapse_icon_for_testing() { return collapse_icon_; }
   views::ImageView* attention_indicator_for_testing() {
     return attention_indicator_;
@@ -141,6 +143,10 @@ class TabGroupHeaderView : public views::FlexLayoutView,
   bool needs_attention_ = false;
   bool is_shared_ = false;
 
+  // In vertical mode, a folder glyph carries the user-selected group hue while
+  // the header itself stays on a neutral rail surface.
+  const raw_ptr<views::ImageView> folder_icon_ = nullptr;
+
   // The sync icon that is displayed in the tab group header of saved groups in
   // the tabstrip.
   const raw_ptr<views::ImageView> sync_icon_ = nullptr;
diff --git a/chrome/browser/ui/views/tabs/common/tab_strip_link_drag_browsertest.cc b/chrome/browser/ui/views/tabs/common/tab_strip_link_drag_browsertest.cc
index 7355525fbfb97dc729232ea8518b2e67eb5e9456..885cbf743c7d4b407061019b04755b8462885500 100644
--- a/chrome/browser/ui/views/tabs/common/tab_strip_link_drag_browsertest.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_strip_link_drag_browsertest.cc
@@ -8,6 +8,7 @@
 #include "base/test/run_until.h"
 #include "base/test/scoped_feature_list.h"
 #include "chrome/browser/ui/browser_tabstrip.h"
+#include "chrome/browser/ui/color/chrome_color_id.h"
 #include "chrome/browser/ui/tabs/features.h"
 #include "chrome/browser/ui/tabs/tab_group_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
@@ -18,9 +19,11 @@
 #include "chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h"
 #include "chrome/browser/ui/views/tabs/common/pinned_tab_container_view.h"
 #include "chrome/browser/ui/views/tabs/common/split_tab_view.h"
+#include "chrome/browser/ui/views/tabs/common/tab_group_header_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_group_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_view.h"
 #include "chrome/browser/ui/views/tabs/common/unpinned_tab_container_view.h"
+#include "chrome/browser/ui/views/tabs/shared/tab_strip_flat_edge_button.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_bottom_container.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_top_container.h"
 #include "chrome/browser/ui/views/test/vertical_tabs_browser_test_mixin.h"
@@ -34,6 +37,10 @@
 #include "ui/display/screen.h"
 #include "ui/gfx/animation/animation_test_api.h"
 #include "ui/gfx/geometry/point_f.h"
+#include "ui/views/background.h"
+#include "ui/views/controls/image_view.h"
+#include "ui/views/layout/layout_types.h"
+#include "ui/views/layout/proposed_layout.h"
 #include "ui/views/view_utils.h"
 
 class TabStripLinkDragTest
@@ -362,6 +369,54 @@ IN_PROC_BROWSER_TEST_P(TabStripLinkDragTest, DropInCollapsedGroups) {
   }
 }
 
+IN_PROC_BROWSER_TEST_P(TabStripLinkDragTest,
+                       ExpandedRailKeepsZenNavigationCues) {
+  if (GetParam() != TabStripOrientation::kVertical) {
+    GTEST_SKIP();
+  }
+
+  EnsureTabCount(2);
+  tab_strip_model()->SetTabPinned(0, true);
+  auto* const vertical_region =
+      views::AsViewClass<VerticalTabStripRegionView>(region_view());
+  ASSERT_TRUE(vertical_region);
+  vertical_region->OnResize(1000, true);
+  RunScheduledLayouts();
+
+  ASSERT_EQ(1u, WaitForPinnedTabs(1).size());
+  ASSERT_EQ(1u, WaitForTabs(1).size());
+  EXPECT_TRUE(vertical_region->tabs_separator_for_testing()->GetVisible());
+  EXPECT_GT(vertical_region->tabs_separator_for_testing()->width(), 0);
+
+  ASSERT_FALSE(vertical_region->GetBottomContainer()->children().empty());
+  auto* const new_tab_button = views::AsViewClass<TabStripFlatEdgeButton>(
+      vertical_region->GetBottomContainer()->children().front());
+  ASSERT_TRUE(new_tab_button);
+  // TabStripFlatEdgeButton clears its public text when the label is hidden.
+  EXPECT_FALSE(new_tab_button->GetText().empty());
+}
+
+IN_PROC_BROWSER_TEST_P(TabStripLinkDragTest,
+                       GroupHeaderUsesNeutralFolderTreatment) {
+  if (GetParam() != TabStripOrientation::kVertical) {
+    GTEST_SKIP();
+  }
+
+  EnsureTabCount(2);
+  tab_strip_model()->AddToNewGroup({0, 1});
+  RunScheduledLayouts();
+
+  TabGroupHeaderView* const header = WaitForGroupView()->group_header();
+  ASSERT_TRUE(header->GetBackground());
+  EXPECT_EQ(header->GetBackground()->color(),
+            header->GetWidget()->ShouldPaintAsActive()
+                ? kColorTabBackgroundInactiveFrameActive
+                : kColorTabBackgroundInactiveFrameInactive);
+  ASSERT_TRUE(header->folder_icon_for_testing());
+  EXPECT_TRUE(
+      header->folder_icon_for_testing()->GetImageModel().IsVectorIcon());
+}
+
 IN_PROC_BROWSER_TEST_P(TabStripLinkDragTest, DropInPinnedTabs) {
   tab_strip_model()->AppendWebContents(
       content::WebContents::Create(
@@ -401,6 +456,57 @@ IN_PROC_BROWSER_TEST_P(TabStripLinkDragTest, DropInPinnedTabs) {
   }
 }
 
+IN_PROC_BROWSER_TEST_P(TabStripLinkDragTest, PinnedTileGridGeometry) {
+  if (GetParam() != TabStripOrientation::kVertical) {
+    GTEST_SKIP();
+  }
+
+  EnsureTabCount(5);
+  for (int index = 0; index < tab_strip_model()->count(); ++index) {
+    tab_strip_model()->SetTabPinned(index, true);
+  }
+  ASSERT_EQ(5u, WaitForPinnedTabs(5).size());
+
+  const views::ProposedLayout two_column_layout =
+      pinned_container()->CalculateProposedLayout(views::SizeBounds(151, {}));
+  ASSERT_EQ(5u, two_column_layout.child_layouts.size());
+  EXPECT_EQ(two_column_layout.child_layouts[0].bounds.y(),
+            two_column_layout.child_layouts[1].bounds.y());
+  EXPECT_LT(two_column_layout.child_layouts[1].bounds.y(),
+            two_column_layout.child_layouts[2].bounds.y());
+  EXPECT_EQ(44, two_column_layout.child_layouts[0].bounds.height());
+  EXPECT_EQ(8, two_column_layout.child_layouts[1].bounds.x() -
+                   two_column_layout.child_layouts[0].bounds.right());
+  EXPECT_EQ(8, two_column_layout.child_layouts[2].bounds.y() -
+                   two_column_layout.child_layouts[0].bounds.bottom());
+
+  const views::ProposedLayout three_column_layout =
+      pinned_container()->CalculateProposedLayout(views::SizeBounds(260, {}));
+  ASSERT_EQ(5u, three_column_layout.child_layouts.size());
+  EXPECT_EQ(three_column_layout.child_layouts[0].bounds.y(),
+            three_column_layout.child_layouts[2].bounds.y());
+  EXPECT_LT(three_column_layout.child_layouts[2].bounds.y(),
+            three_column_layout.child_layouts[3].bounds.y());
+  EXPECT_EQ(8, three_column_layout.child_layouts[2].bounds.x() -
+                   three_column_layout.child_layouts[1].bounds.right());
+
+  auto* const vertical_region =
+      views::AsViewClass<VerticalTabStripRegionView>(region_view());
+  ASSERT_TRUE(vertical_region);
+  vertical_region->OnResize(-1000, true);
+  RunScheduledLayouts();
+  const views::ProposedLayout collapsed_layout =
+      pinned_container()->CalculateProposedLayout(views::SizeBounds(44, {}));
+  ASSERT_EQ(5u, collapsed_layout.child_layouts.size());
+  EXPECT_EQ(32, collapsed_layout.child_layouts[0].bounds.width());
+  for (const views::ChildLayout& child_layout :
+       collapsed_layout.child_layouts) {
+    EXPECT_EQ(0, child_layout.bounds.x());
+  }
+  EXPECT_EQ(8, collapsed_layout.child_layouts[1].bounds.y() -
+                   collapsed_layout.child_layouts[0].bounds.bottom());
+}
+
 IN_PROC_BROWSER_TEST_P(TabStripLinkDragTest, GetLinkDropBoundsNoShift) {
   AppendTab();
   AppendTab();
diff --git a/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc b/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc
index db53e40e591295311569835fae59eb340ad776a4..02efe7ce34edca5c8900702c281f5c6e29d9f066 100644
--- a/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc
@@ -158,8 +158,6 @@ views::ProposedLayout TabStripViewLayout::CalculateVerticalLayout(
   views::ScrollView* unpinned_tabs_scroll_view =
       tab_strip_view->unpinned_tabs_scroll_view();
   views::Separator* tabs_separator = tab_strip_view->GetTabsSeparator();
-  const bool is_collapsed = tab_strip_view->is_collapsed_;
-
   if (!size_bounds.width().is_bounded()) {
     return layouts;
   }
@@ -182,9 +180,10 @@ views::ProposedLayout TabStripViewLayout::CalculateVerticalLayout(
   const int unpinned_preferred_height =
       unpinned_tabs_scroll_view->GetPreferredSize(size_bounds).height();
 
-  const bool should_show_separator = pinned_preferred_height != 0 &&
-                                     unpinned_preferred_height != 0 &&
-                                     is_collapsed;
+  // Keep persistent destinations visually distinct from ordinary tabs in both
+  // expanded and collapsed layouts.
+  const bool should_show_separator =
+      pinned_preferred_height != 0 && unpinned_preferred_height != 0;
 
   // If the height is bounded, calculate the available space for laying out the
   // pinned and unpinned containers.
diff --git a/chrome/browser/ui/views/tabs/common/tab_view.cc b/chrome/browser/ui/views/tabs/common/tab_view.cc
index e8d8cb4facb9428bab15389a8fb137ada0982a21..1d439b37f141df0c851b89a553c149108367362e 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view.cc
@@ -497,8 +497,8 @@ bool TabView::IsHoverAnimationActive() const {
 }
 
 std::optional<SkColor> TabView::GetBackgroundColor() {
-  if (active_ || IsHoverAnimationActive() ||
-      should_fill_background_tab_color_) {
+  if ((orientation_ == TabStripOrientation::kVertical && pinned_) || active_ ||
+      IsHoverAnimationActive() || should_fill_background_tab_color_) {
     return tab_styling()->CalculateTargetColors().background_color;
   }
   return std::nullopt;
@@ -1105,16 +1105,19 @@ void TabView::UpdateBorder() {
   }
 
   if (pinned_) {
+    const int border_thickness =
+        GetLayoutConstant(LayoutConstant::kVerticalTabPinnedBorderThickness);
     if (split_) {
       // Insets for border handled by the `SplitTabView`.
-      SetBorder(views::CreateEmptyBorder(gfx::Insets(GetLayoutConstant(
-          LayoutConstant::kVerticalTabPinnedBorderThickness))));
-    } else {
+      SetBorder(views::CreateEmptyBorder(gfx::Insets(border_thickness)));
+    } else if (ShouldPaintActivePinnedOutline()) {
       SetBorder(views::CreateRoundedRectBorder(
-          GetLayoutConstant(LayoutConstant::kVerticalTabPinnedBorderThickness),
+          border_thickness,
           GetLayoutConstant(LayoutConstant::kVerticalTabCornerRadius),
-          IsFrameActive() ? kColorVerticalTabPinnedOutline
-                          : kColorTabDividerFrameInactive));
+          kColorVerticalTabPinnedOutline));
+    } else {
+      // Match the active border's inset so selection never shifts the favicon.
+      SetBorder(views::CreateEmptyBorder(gfx::Insets(border_thickness)));
     }
   } else if (GetBorder()) {
     SetBorder(nullptr);
@@ -1133,6 +1136,11 @@ void TabView::UpdateColors() {
   alert_indicator_->OnParentTabButtonColorChanged();
 
   UpdateBorder();
+  if (orientation_ == TabStripOrientation::kVertical && split_) {
+    if (auto* split_view = views::AsViewClass<SplitTabView>(parent())) {
+      split_view->UpdateBorder();
+    }
+  }
 
   // TODO(crbug.com/465159185): Update focus ring colors.
   SchedulePaint();
diff --git a/chrome/browser/ui/views/tabs/common/tab_view.h b/chrome/browser/ui/views/tabs/common/tab_view.h
index 04458f07af7f0aba0cd5446383df7074065330f2..ea53db9c68ca0c0de06e02145804daccb8ca9525 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view.h
+++ b/chrome/browser/ui/views/tabs/common/tab_view.h
@@ -99,6 +99,9 @@ class TabView : public views::View,
   float radial_highlight_opacity() { return radial_highlight_opacity_; }
   const tabs::TabData& data() const { return tab_data_; }
   bool IsActive() const { return active_; }
+  bool ShouldPaintActivePinnedOutline() const {
+    return orientation_ == TabStripOrientation::kVertical && pinned_ && active_;
+  }
   bool IsClosing() const { return !collection_node_; }
   bool split() const { return split_; }
   const tabs::TabInterface* GetTabInterface() const;
diff --git a/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc b/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc
index 0d800d7afb253c4a0f1dec5e07d33959d7882ced..ac9e68133224396d7a7440d49e30255ac529b723 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view_browsertest.cc
@@ -12,6 +12,7 @@
 #include "build/build_config.h"
 #include "chrome/browser/media/webrtc/media_capture_devices_dispatcher.h"
 #include "chrome/browser/performance_manager/public/user_tuning/user_performance_tuning_manager.h"
+#include "chrome/browser/resource_coordinator/tab_lifecycle_unit_external.h"
 #include "chrome/browser/tab_group_sync/tab_group_sync_service_factory.h"
 #include "chrome/browser/ui/animation/browser_animation_controller.h"
 #include "chrome/browser/ui/browser_element_identifiers.h"
@@ -27,9 +28,11 @@
 #include "chrome/browser/ui/views/tabs/common/split_tab_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/hovercard/tab_hover_card_bubble_view.h"
+#include "chrome/browser/ui/views/tabs/tab/tab_accessibility.h"
 #include "chrome/browser/ui/views/tabs/tab/tab_close_button.h"
 #include "chrome/browser/ui/views/tabs/tab/tab_icon.h"
 #include "chrome/browser/ui/views/test/vertical_tabs_browser_test_mixin.h"
+#include "chrome/browser/ui/window_metadata/window_metadata_controller.h"
 #include "chrome/grit/generated_resources.h"
 #include "chrome/test/base/in_process_browser_test.h"
 #include "chrome/test/base/ui_test_utils.h"
@@ -577,6 +580,82 @@ IN_PROC_BROWSER_TEST_F(TabViewTest, PinnedTabsRenderBorder) {
   EXPECT_FALSE(unpinned_tab->GetBorder());
 }
 
+IN_PROC_BROWSER_TEST_F(TabViewTest, PinnedTabsUseStableActiveOnlyOutline) {
+  AppendPinnedTab();
+  AppendPinnedTab();
+  ASSERT_EQ(2u, pinned_collection_node()->children().size());
+
+  TabView* first = views::AsViewClass<TabView>(
+      pinned_collection_node()->children()[0]->view());
+  TabView* second = views::AsViewClass<TabView>(
+      pinned_collection_node()->children()[1]->view());
+  ASSERT_TRUE(first->GetBorder());
+  ASSERT_TRUE(second->GetBorder());
+
+  EXPECT_FALSE(first->ShouldPaintActivePinnedOutline());
+  EXPECT_TRUE(second->ShouldPaintActivePinnedOutline());
+  EXPECT_EQ(gfx::Insets(2), first->GetInsets());
+  EXPECT_EQ(first->GetInsets(), second->GetInsets());
+  EXPECT_TRUE(first->GetBackgroundColor().has_value());
+  EXPECT_TRUE(second->GetBackgroundColor().has_value());
+
+  tab_strip_model()->ActivateTabAt(0);
+  ASSERT_TRUE(base::test::RunUntil([&]() {
+    return first->ShouldPaintActivePinnedOutline() &&
+           !second->ShouldPaintActivePinnedOutline();
+  }));
+  EXPECT_EQ(gfx::Insets(2), first->GetInsets());
+  EXPECT_EQ(first->GetInsets(), second->GetInsets());
+}
+
+IN_PROC_BROWSER_TEST_F(TabViewTest,
+                       ExplicitUserUnloadShowsPersistentTabStatus) {
+  ASSERT_TRUE(embedded_test_server()->Start());
+  const GURL original_url =
+      embedded_test_server()->GetURL("example.com", "/title2.html");
+  ASSERT_TRUE(ui_test_utils::NavigateToURL(browser(), original_url));
+  tab_strip_model()->SetTabPinned(0, true);
+
+  tabs::TabInterface* const unloaded_tab = tab_strip_model()->GetTabAtIndex(0);
+  TabView* const unloaded_view = views::AsViewClass<TabView>(
+      pinned_collection_node()->children()[0]->view());
+  TabIcon* const unloaded_icon = views::AsViewClass<TabIcon>(
+      unloaded_view->GetViewByElementId(kTabIconElementId));
+  const std::u16string original_title = unloaded_view->data().title;
+  const ui::ImageModel original_favicon = unloaded_view->data().favicon;
+
+  AppendTab();
+  resource_coordinator::TabLifecycleUnitExternal* const lifecycle =
+      resource_coordinator::TabLifecycleUnitExternal::FromWebContents(
+          unloaded_tab->GetContents());
+  ASSERT_TRUE(lifecycle);
+  ASSERT_TRUE(lifecycle->CanUnloadTab());
+  ASSERT_TRUE(lifecycle->UnloadTab());
+
+  ASSERT_TRUE(base::test::RunUntil([&]() {
+    return unloaded_view->data().is_tab_discarded &&
+           unloaded_view->data().should_show_discard_status &&
+           unloaded_icon->GetShowingDiscardIndicator();
+  }));
+
+  const std::u16string pinned_name = l10n_util::GetStringFUTF16(
+      IDS_TAB_AX_LABEL_PINNED_FORMAT,
+      WindowMetadataController::From(browser())->GetTitleForTab(
+          unloaded_tab->GetHandle()));
+  const std::u16string expected_accessible_name =
+      l10n_util::GetStringFUTF16(IDS_TAB_AX_INACTIVE_TAB, pinned_name);
+  EXPECT_EQ(expected_accessible_name,
+            unloaded_view->GetViewAccessibility().GetCachedName());
+  EXPECT_EQ(expected_accessible_name,
+            tabs::GetAccessibleTabLabel(unloaded_tab, /*is_for_tab=*/true));
+
+  EXPECT_EQ(0, tab_strip_model()->GetIndexOfTab(unloaded_tab));
+  EXPECT_TRUE(unloaded_tab->IsPinned());
+  EXPECT_EQ(original_url, unloaded_tab->GetContents()->GetLastCommittedURL());
+  EXPECT_EQ(original_title, unloaded_view->data().title);
+  EXPECT_EQ(original_favicon, unloaded_view->data().favicon);
+}
+
 IN_PROC_BROWSER_TEST_F(TabViewTest, LogsTabCloseMetrics) {
   base::UserActionTester user_action_tester;
 
diff --git a/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc b/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc
index 6641560e2f254670b98975cfd02c81c3ed5ceea7..d6c41665f0bfe4863475e581ef269268d28ce94d 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc
@@ -14,9 +14,11 @@
 #include "chrome/browser/ui/views/tabs/tab/tab_icon.h"
 #include "chrome/browser/ui/views/tabs/tab/tab_title.h"
 #include "chrome/browser/ui/views/tabs/tab_style_views.h"
+#include "ui/gfx/favicon_size.h"
 
 namespace {
 constexpr int kIconDesignWidth = 16;
+static_assert(kIconDesignWidth == gfx::kFaviconSize);
 constexpr int kTitleMinWidth = 10;
 constexpr int kHorizontalInset = 8;
 constexpr int kDefaultPadding = 4;
diff --git a/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_bottom_container.cc b/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_bottom_container.cc
index 08d127e8a47a42a0d2d13f9a9e7cb21769e08d79..7e4a88a454940b1f68b6f43a8520ec841f5d1daf 100644
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_bottom_container.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_bottom_container.cc
@@ -41,6 +41,8 @@ VerticalTabStripBottomContainer::VerticalTabStripBottomContainer(
           record_new_tab_button_pressed);
 
   new_tab_button_ = AddChildView(std::move(new_tab_button));
+  // Keep the primary action legible whenever the rail has room for its label.
+  new_tab_button_->SetShouldShowLabel(true);
 
   OnCollapseStateChanged(state_controller->GetCollapseState());
   collapsed_state_change_subscription_ =
@@ -51,7 +53,6 @@ VerticalTabStripBottomContainer::VerticalTabStripBottomContainer(
 
 VerticalTabStripBottomContainer::~VerticalTabStripBottomContainer() = default;
 
-
 bool VerticalTabStripBottomContainer::IsPositionInWindowCaption(
     const gfx::Point& point) {
   for (views::View* child : children()) {
diff --git a/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc b/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc
index cbec9de4fa9c629e50e8c13330f50ad2cca235e4..9c19954f20cc1985bd0f1e9f473cfc5b22c61e08 100644
--- a/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc
+++ b/chrome/browser/ui/views/tabs/vertical_tab_style_views.cc
@@ -166,6 +166,12 @@ bool VerticalTabStyleViews::ShouldPaintTabBackgroundColor(
     TabStyle::TabSelectionState selection_state,
     bool has_custom_background,
     bool hovered) const {
+  // Pinned tabs are persistent destinations, so keep a calm tile surface even
+  // while they are inactive. Custom themes still supply the actual colors.
+  if (delegate_->IsPinned()) {
+    return true;
+  }
+
   if (selection_state == TabStyle::TabSelectionState::kActive) {
     return true;
   }
